<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- App theme - AppCompat descendant (used with AppCompatActivity) -->
    <style name="Theme.MedicineReminderApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Basic attributes you can customize -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
        <!-- If you want Material components later, migrate carefully; for now AppCompat keeps AppCompatActivity happy -->
    </style>

</resources>
